import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primaryBrand = Color(0xFF2AD578); // Bamboo Green
  static const Color bambooGreen = Color(0xFF2AD578); // Alias
  static const Color bambooDark = Color(
    0xFF15803d,
  ); // Darker Green for borders/shadows

  // Theme Colors
  static const Color background = Color(
    0xFFF0FDF4,
  ); // Very light mint (funBg alias)
  static const Color funBg = Color(0xFFF0FDF4); // Alias for compatibility
  static const Color surface = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textMain = Color(0xFF102219); // Panda Black
  static const Color textLight = Color(0xFF6B7280);

  // Accents
  static const Color accent = Color(0xFFFF9F1C); // Cheerful Orange
  static const Color accentFun = Color(0xFFFFD336); // Yellow

  // Specifics
  static const Color pandaBlack = Color(0xFF102219); // Deep dark green/black
}
